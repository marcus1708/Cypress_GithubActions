name: Curso Github Actions + Cypress

on:
  push:
    branches:
      - main

jobs:
  cypress-run:
    runs-on: ubuntu-22.04

    steps:
      # Faz o checkout do código
      - name: Checkout
        uses: actions/checkout@v4

      # Configura o Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # Instala as dependências do projeto
      - name: Instalar dependências
        run: npm install

      # Executa os testes no Electron
      - name: Cypress run no Electron
        uses: cypress-io/github-action@v6
        with:
          browser: electron

      # Executa os testes no Firefox
      - name: Cypress run no Firefox
        uses: cypress-io/github-action@v6
        with:
          browser: firefox

      # Executa os testes no Chrome
      - name: Cypress run no Chrome
        uses: cypress-io/github-action@v6
        with:
          browser: chrome
